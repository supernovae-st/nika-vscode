# The nika-spec commit the generated surfaces are projected from
# (verbStarters · authoringShapes · design-tokens). The CI parity gate
# checks against THIS rev — never a floating HEAD, so a spec merge can
# never red unrelated PRs here (the registry's SPEC_PIN law). The daily
# spec-pin-heal advances it: re-project → one idempotent PR → this
# repo's own gates judge. Bump deliberately the same way.
f308fa32e28c4399aad96fc63f09dab4e57a75a7
