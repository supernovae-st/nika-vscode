# The supernovae-st/nika tag (or commit) the real-binary suites
# (contract · journeyReal · semanticDocReal · graph_format 3 · the
# dead-envelope refusals) are proven against. A release tag is
# DOWNLOADED (checksum-verified · the same asset a user installs); a
# commit sha is BUILT from source. Never « latest release » as a
# floating pin. Bump deliberately, with the reader.
v0.116.2
